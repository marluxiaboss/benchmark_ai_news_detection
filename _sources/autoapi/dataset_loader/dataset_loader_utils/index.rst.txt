dataset_loader.dataset_loader_utils
===================================

.. py:module:: dataset_loader.dataset_loader_utils


Functions
---------

.. autoapisummary::

   dataset_loader.dataset_loader_utils.create_train_from_dataset
   dataset_loader.dataset_loader_utils.filter_duplicates
   dataset_loader.dataset_loader_utils.create_splits


Module Contents
---------------

.. py:function:: create_train_from_dataset(dataset: datasets.Dataset) -> datasets.DatasetDict

   Create a train split from a dataset. We go from Dataset to DatasetDict.

   Parameters:
   ----------
       dataset : Dataset
           The dataset to create the train split from

   Returns:
   -------
       DatasetDict
           The dataset with the train split


.. py:function:: filter_duplicates(dataset: datasets.Dataset, text_field: str) -> datasets.Dataset

   Filter duplicates in the dataset based on the text_field.

   Parameters:
   ----------
       dataset: Dataset
           The dataset to filter duplicates from
       text_field: str
           The field to use for filtering duplicates

   Returns:
   -------
       Dataset
           The dataset without duplicates


.. py:function:: create_splits(dataset: datasets.Dataset, train_size: float, eval_size: float, test_size: float) -> datasets.DatasetDict

   Create train, eval and test splits from a dataset.

   Parameters:
   ----------
       dataset: Dataset
           The dataset to create the splits from
       train_size: float
           The size of the train split
       eval_size: float
           The size of the eval split
       test_size: float
           The size of the test split

   Returns:
   ----------
       DatasetDict
           The dataset with the train, eval and test splits


